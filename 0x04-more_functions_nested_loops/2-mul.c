#include "main.h"


/**
* mul - multiply 2 int
* @a: first int
* @b: second int
* Return: addition of the multiplication
*/
int mul(int a, int b);
{

	int a, b;

	printf(“Enter the values of a and b”);

	scanf(“ % d % d”, &a, &b);

	int x = a * b;

	printf(“ % d”, x);
	return (0);
}
