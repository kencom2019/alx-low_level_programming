#include "main.h"


/**
* mul - multiply 2 int
* @a: first int
* @b: second int
* Return: addition of the multiplication
*/
int mul(int a, int b);
{
	int a, b, mul;

	printf("Enter the value of two numbers");
	scanf("%d %d", &a, &b);
	mul = a * b
	printf("%d", mul)
	return (0);
}
